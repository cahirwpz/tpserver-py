
try:
	import psyco
except ImportError:
	pass
	
# Database config
#dbconfig = "mysql://tp:tp-password@localhost/tp"
dbconfig = "sqlite:///tp.db"

# Output all the SQL statements
dbecho = False

# Admin users
admin = (1,)

usercreation = True
